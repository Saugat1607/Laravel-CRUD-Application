# Laravel-CRUD-Application
This is a CRUD (Create, Read, Update, Delete) application developed using the Laravel framework. The project demonstrates the use of Laravel’s MVC architecture, routing, controllers, and Eloquent ORM for efficient database management. Blade templates are used to build a clean and user-friendly interface
